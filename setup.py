from setuptools import setup

setup(name='cerebro', 
      version='0.1',
      description='Package for learning from streams of data',
      url = 'https://github.com/jmread/cerebro',
      author = 'Jesse Read',
      license='GPL3',
      packages=['cerebro']
      )
